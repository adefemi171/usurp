[Unit]
Description=Install Usurp-only PostgreSQL ingress restrictions before Docker
Before=docker.service
After=network-pre.target
[Service]
Type=oneshot
ExecStart=/opt/usurp-data/firewall.sh
RemainAfterExit=true
[Install]
WantedBy=multi-user.target
